root = true

[Petting/**.cs]
# The API intentionally uses one flat PetAnyone namespace for every file,
# including the files grouped in the Petting folder.
dotnet_style_namespace_match_folder = false
dotnet_diagnostic.IDE0130.severity = none
